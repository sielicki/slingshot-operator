---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: cxidevices.cxi.hpe.com
spec:
  group: cxi.hpe.com
  names:
    kind: CXIDevice
    listKind: CXIDeviceList
    plural: cxidevices
    singular: cxidevice
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.node
      name: Node
      type: string
    - jsonPath: .status.device
      name: Device
      type: string
    - jsonPath: .status.numaNode
      name: NUMA
      type: integer
    - jsonPath: .status.switchID
      name: Switch
      type: integer
    - jsonPath: .status.linkState
      name: Link
      type: string
    - jsonPath: .status.health.status
      name: Health
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: |-
          CXIDevice is the Schema for the cxidevices API.
          It represents a discovered CXI device on a node and is auto-managed by the operator.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              CXIDeviceSpec defines the desired state of CXIDevice.
              This is intentionally minimal as CXIDevice is primarily a status resource.
            type: object
          status:
            description: CXIDeviceStatus defines the observed state of CXIDevice
            properties:
              cassiniGeneration:
                description: CassiniGeneration is the hardware generation (1, 2, or
                  3)
                format: int32
                type: integer
              conditions:
                description: Conditions represent the current state of the CXIDevice
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              device:
                description: Device is the device name (e.g., "cxi0")
                type: string
              driverVersion:
                description: DriverVersion is the loaded driver version
                type: string
              health:
                description: Health contains health information
                properties:
                  lastCheck:
                    description: LastCheck is the timestamp of the last health check
                    format: date-time
                    type: string
                  message:
                    description: Message provides additional context about the health
                      status
                    type: string
                  status:
                    description: Status is the overall health status
                    enum:
                    - Healthy
                    - Degraded
                    - Unhealthy
                    - Unknown
                    type: string
                type: object
              linkSpeed:
                description: LinkSpeed is the link speed (e.g., "200Gbps")
                type: string
              linkState:
                description: LinkState is the current link state
                enum:
                - up
                - down
                - unknown
                type: string
              nid:
                description: NID is the Node ID (20-bit fabric address) for this device
                format: int32
                type: integer
              node:
                description: Node is the name of the node where this device exists
                type: string
              numaNode:
                description: NUMANode is the NUMA node the device is attached to
                format: int32
                type: integer
              pciAddress:
                description: PCIAddress is the PCI address of the device (e.g., "0000:41:00.0")
                type: string
              retryHandler:
                description: RetryHandler contains retry handler status for this device
                properties:
                  enabled:
                    description: Enabled indicates if retry handling is active for
                      this device
                    type: boolean
                  policy:
                    description: Policy is the active retry policy name
                    type: string
                  stats:
                    description: Stats contains retry statistics
                    properties:
                      nackRetries:
                        description: NackRetries is the number of NACK-triggered retries
                        format: int64
                        type: integer
                      pendingRetries:
                        description: PendingRetries is the current number of pending
                          retries
                        format: int32
                        type: integer
                      timeoutRetries:
                        description: TimeoutRetries is the number of timeout-triggered
                          retries
                        format: int64
                        type: integer
                      totalRetries:
                        description: TotalRetries is the total number of retries performed
                        format: int64
                        type: integer
                    type: object
                type: object
              switchID:
                description: |-
                  SwitchID is the switch ID derived from NID for topology awareness
                  This identifies which switch in the dragonfly topology this device connects to
                format: int32
                type: integer
              type:
                default: pf
                description: Type indicates if this is a physical function (pf) or
                  virtual function (vf)
                enum:
                - pf
                - vf
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
